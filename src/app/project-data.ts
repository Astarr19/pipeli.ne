export interface ProjectData {
    records: {
        fields: {
            companyName: string,
            companyWebsite: string,
            country: string,
            alignment: string,
            theme: string,
            summary: string
        }
    }
}
